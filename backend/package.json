{
  "name": "uwmctools",
  "version": "2.8.5",
  "description": "convert data from unlimitedworld.de, mostly used for the tools on https://uwmc.lergin.de",
  "homepage": "https://github.com/Lergin/uwmctools",
  "bugs": {
    "url": "https://github.com/Lergin/uwmctools/issues"
  },
  "license": "MIT",
  "author": {
    "name": "Malte Laukötter",
    "email": "lergin@gmx.de",
    "url": "Lergin.de"
  },
  "main": "src/main.js",
  "scripts": {
    "build": "babel src -d lib --copy-files",
    "test": "babel src -d lib --copy-files && node lib/test.js"
  },
  "repository": "Lergin/uwmctools",
  "engines": {
    "node": ">=7.0.0"
  },
  "dependencies": {
    "request": "^2.73.0"
  },
  "devDependencies": {
    "babel": "^6.5.2",
    "babel-preset-es2017": "^6.16.0",
    "eslint": "^3.9.0",
    "eslint-config-google": "^0.7.0"
  }
}
